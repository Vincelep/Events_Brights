<div class="text-center" style="margin-top: 5vh">
  <h2>Espace Evènement</h2>
 </div>

  <div class="card border-warning  m-5">

    <div class="card-header text-center">
      <h1><i class="fas fa-sun"></i> <%= @event.title %></h1>
    </div>
    <div class="card-body text-primary">
        <p class="m-2">La liste des participants à ton Event.</p>
        <div class="card-body text-primary">
          <ul class="card-text list-group">
            <li class="list-group-item">
              prénom : <%= @attendance.attendee.first_name %><br>
              Nom : <%= @attendance.attendee.last_name %><br>
              Email : <%= @attendance.attendee.email %><br></li></br><br>
            <p class="text-right">
              <%= link_to "Edit", edit_event_path(params[:event_id]), :class => "btn btn-outline-success btn-sm" %>
              <%= link_to "Delete", event_path(params[:event_id]), :class => "btn btn-outline-danger btn-sm", :data => { :confirm => "Are you sure?" }, :method => "delete" %>
            </p>
          </ul>
        </div>
    </div>


